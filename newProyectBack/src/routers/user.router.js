const {Router} = require("express")

const routerUser = Router()

/* routerUser.use("", )
routerUser.use("", )
routerUser.use("", )
routerUser.use("", )
routerUser.use("", )
 */
module.exports = routerUser